{
  "framework": "vite",
  "installCommand": "npm install --no-save --legacy-peer-deps --omit=optional || npm install --force --legacy-peer-deps --omit=optional",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "installCommand": "npm install --no-save --legacy-peer-deps --omit=optional || npm install --force --legacy-peer-deps --omit=optional"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "github": {
    "silent": true
  }
}
